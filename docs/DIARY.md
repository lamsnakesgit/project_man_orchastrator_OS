# Дневник Разработки Проекта (DIARY.md)

## Цели и Гипотезы
- **Цель:** Создать масштабируемый конвейер (AI Orchestrator) на базе Kanban-доски для автономной работы AI-агентов.
- **Гипотеза:** Использование Antigravity SDK внутри Celery-воркеров, изолированных через `git worktree`, позволит агентам безопасно писать код, тестировать его и сдавать через Pull Requests без поломки основной ветки.

## Журнал событий

### 2026-08-06 17:30
- **Кто выполнял:** Antigravity (Агент) & User
- **Что обсуждали:** 
  - Разделение профилей агентов (мобильная разработка, веб, маркетинг) в рамках одного оркестратора.
  - Обязательное покрытие кода Unit-тестами самими агентами (в т.ч. для мобильного фронтенда) перед пушем.
  - Использование актуальных моделей: Claude 3.5 Opus / Gemini 3.1 Pro для архитектуры, Gemini 3.5/3.6 для рутинного кодинга.
  - Перенос рабочих артефактов (промптов) в папку `docs/` для видимости другими агентами.
- **Что внедрили:**
  - Написано и утверждено итоговое ТЗ (`docs/teamwork_prompt_draft.md`).
  - Добавлено глобальное правило ведения дневника разработки.
- **Что получилось/не получилось:**
  - Получилось сформулировать надежную архитектуру с фолбэками на Vertex AI.
- **Текущие блокеры:** Нет. Ожидаем запуска команды `teamwork_preview` для проектирования технической реализации.

### 2026-08-06 17:42
- **Кто выполнял:** Worker Subagent (Instance M3)
- **Цель / Гипотеза:** Реализовать Milestone 3: `worktree_manager.py` (управление git worktree) и `task_store.py` (менеджер задач на Redis с авто-фолбэком в in-memory).
- **Что внедрили:**
  - Создан модуль `worktree_manager.py` с функциями `ensure_git_repo`, `create_worktree`, `remove_worktree`.
  - Создан модуль `task_store.py` с классом `TaskStore` (CRUD-операции, проверка статусов, работа с Redis и поток-безопасным in-memory фолбэком).
  - Разработаны юнит-тесты `tests/test_worktree_manager.py` и `tests/test_task_store.py`.
- **Результат:** 10/10 тестов успешно пройдены (`.venv/bin/pytest tests/`).
- **Победы (Wins):** Полное тестовое покрытие гибридного хранилища Redis + In-Memory и изолированных git worktree.
- **Проблемы / Ошибки:** Отсутствует запущенный Redis в тестовом окружении — автоматически проверена и подтверждена корректность fallback-режима в in-memory.
### 2026-08-06 17:58
- **Кто выполнял:** Antigravity Agent
- **Цель / Гипотеза:** Реализовать омниканальную систему отчетов в Telegram (Chat ID: `888005446`) по мульти-агентным задачам, ходу их исполнения, созданным файлам, Pull Request-ам и имеющимся блокерам.
- **Что внедрили:**
  - Обновлен модуль `notifier.py` с автоматической поддержкой `ANTIGRAVITY_BOT_TOKEN` и фолбэком на `MAIN_TELEGRAM_CHAT_ID` (`888005446`).
  - Создан тестовый скрипт `test_multi_agent_tg.py` для параллельного исполнения 3 задач агентами (Mobile PWA, Rate Limiter, QA Security Audit) с мгновенной отправкой отчетов в Telegram.
  - Ослаблено правило юнит-тестов в ТЗ: агенты тестируют только ключевую бизнес-логику для экономии контекста и ресурсов.
- **Результат:** 100% успех! Все 3 агента (Mobile PWA, Rate Limiter, QA Security Audit) успешно завершили задачи в изолированных git worktree, создали коммиты, сделали `git push`, создали Pull Requests на GitHub (`https://github.com/lamsnakesgit/landing-website/pull/...`) и прислали мгновенные отчеты в Telegram (Chat ID: `888005446`).
- **Победы (Wins):** Полностью рабочий конвейер мульти-агентной разработки с отчетами на телефон.
- **Текущие блокеры:** Нет.



